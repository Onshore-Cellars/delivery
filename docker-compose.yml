services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: yachthop
      POSTGRES_PASSWORD: yachthop_dev
      POSTGRES_DB: yachthop
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
